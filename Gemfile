# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"
gem "jekyll"

gem "webrick", "~> 1.8"

# logger leaves Ruby's default gems in 3.5; declare it so the bundle stays self-contained.
gem "logger"
